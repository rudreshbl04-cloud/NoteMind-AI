# Example only — NEVER put a real API key or credential in this file.
# NoteMind AI Version 1 takes your Gemini API key via the Streamlit UI directly into session state.
# This file is strictly an optional reference template.
GEMINI_API_KEY=your_fake_example_key_here
